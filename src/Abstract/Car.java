package Abstract;

public class Car extends Vehicle {
    /**if car or any class must extend and abstract class (Vehicle), it must become an abstract itself(add abstract) or it must implement
    (override the abstract methods in the super class) method turn right from abstract class vehicle
     it can not create an abstract method of its own, if it does, it has to become and abstract class itself
     */
    @Override
    public void turnRight(){

    }

}
